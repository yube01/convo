const Messages = () => {
  return (
    <div className="messages">
      {/* {messages.map((m) => (
          <Message message={m} key={m.id} />
        ))} */}
    </div>
  );
};

export default Messages;
